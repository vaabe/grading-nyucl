#!/usr/bin/env bash

if [ "$#" -ne 2 ]; then
	echo "Invalid parameters"
	echo "Usage: grade <dir> <-s|-g|-f>"
	exit
fi

reportdir=$1
mode=$2
srcdir="${HOME}/bin/grading"

make -C "${HOME}/bin/grading" start
